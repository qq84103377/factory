//test
export const getCount = state => {
    return state.updataPwdVisible
}

export const facs = state => state.facs;

export const userInfo = state => state.userInfo;

export const userLogin = state => state.userLogin;
